<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <div class="panel panel-primary">
       <div class="panel-heading">
        <% if @project.new_record? %>
          <h3>Projekt létrehozása</h3>
        <% else %>
          <h3>Projekt szerkesztése</h3>
        <% end %>
      </div>
      <div class="panel-body">
        <%= form_for(@project) do |f| %>
          <% if @project.errors.any? %>
            <div id="error_explanation">
              <h2><%= @project.errors.count %> hibát kérem javítson ki:</h2>

              <ul>
              <% @project.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
              </ul>
            </div>
          <% end %>
          <div class="field">
            <%= f.label :name, "A projekt neve" %>  
            <%= f.text_field :name, :autofocus => true, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :description, "A projekt adatai" %> 
            <%= f.text_area :description, :rows => "10", class: 'form-control' %>
          </div>
          <% if @project.new_record? %>
            <%= f.submit "Projekt létrehozása", class: 'btn btn-primary btn-lg btn-block' %>
          <% else %>
            <%= f.submit "Projekt adatok frissítése", class: 'btn btn-primary btn-lg btn-block' %>
          <% end %>
        <% end %>
      </div> <!--panel-body -->
    </div> <!--panel-primary -->
  </div> <!--col-md4 -->
</div> <!--row -->